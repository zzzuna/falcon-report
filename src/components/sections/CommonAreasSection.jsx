import { ListSection } from './ListSection';

export function CommonAreasSection({ items }) {
    return <ListSection id="common-areas" title="Common Areas & Facilities" items={items} pageBreak={true} />;
}
